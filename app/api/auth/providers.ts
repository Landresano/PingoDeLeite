// Remova a diretiva 'use server' se estiver presente
// Este arquivo deve conter apenas provedores, não ações do servidor

// Provedores de autenticação
export const providers = {
  credentials: {
    name: "Credentials",
    type: "credentials",
  },
}

